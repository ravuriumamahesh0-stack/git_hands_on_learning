
def sub(a,b=10):  
    a = a 
    b = b
    return a-b

if __name__ == "__main__": # when we import file the entire file runs once so this calling also execute once to that i wrote this 

    print(sub(10,4))